# Shared production defaults. Loaded by `next build` and `next start`.
#
# On a real host (Railway, Vercel, a VPS) set the real values as service
# variables there — those win over this file and never enter the repository.

API_URL=https://api.aviro.ng/api
