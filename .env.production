# Production defaults, loaded by `next build` and `next start`.
#
# API_URL is deliberately NOT set here. A placeholder value would be silently
# inherited when the real environment variable is missing, and the app would
# point at a domain that does not exist instead of failing at startup with a
# clear message. Set it on the host — Railway or Vercel service variables —
# where it belongs.
